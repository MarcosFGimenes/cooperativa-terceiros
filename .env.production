# Variáveis usadas no deploy de produção (sem segredos).
# Preencha as chaves do Firebase via painel da hospedagem.
NEXT_PUBLIC_FIRESTORE_FORCE_LONG_POLLING=true
NEXT_PUBLIC_FIRESTORE_USE_FETCH_STREAMS=false
