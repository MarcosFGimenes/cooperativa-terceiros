rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    // DEV: qualquer usuário autenticado pode ler/escrever
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
